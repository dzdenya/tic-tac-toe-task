server:
  port: 8082

spring:
  application:
    name: game-session-service
  r2dbc:
    url: r2dbc:h2:mem:///game_session;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always

game-engine:
  base-url: http://localhost:8081
